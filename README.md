# Flat Renovation Cost Tracker

🚧 **Work in progress.** This app is under active development — features, data model, and UI are still being built out.

A Vite + React + TypeScript app for tracking the cost of renovating a flat, backed by Firebase (Realtime Database + Auth). See `CLAUDE.md` for architecture and conventions.
